import Breadcrumb from '../../../components/common/Breadcrumb';
import { breadcrumbItems } from '../shopData';

function ShopBreadcrumb() {
  return <Breadcrumb items={breadcrumbItems} />;
}

export default ShopBreadcrumb;
